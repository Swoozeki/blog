extends layout

block body
    #content
        each article in articles
            h3
                a(href=article.url)= article.title
            span!= article['marked-excerpt']
    if next
        button#next Next